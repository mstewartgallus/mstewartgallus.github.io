type Index implements Node {
  id: ID!
  label: String!
}

type Link implements Node @childOf(types: ["Post"]) {
  id: ID!
  index: Index!
  post: Post!
  next: Link
  previous: Link
  date: Date! @dateformat
}
